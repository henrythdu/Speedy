name = "config"
layer = 1
purpose = "TOML configuration: XDG-compliant load/save (config_path, load, save), the Config struct, and ThemeColors (RGBA palette) used app-wide."
provides = ["Config", "config_path", "load", "save", "ConfigLoadError", "ConfigSaveError"]
requires = ["engine"]
