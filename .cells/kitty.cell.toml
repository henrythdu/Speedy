name = "kitty"
layer = 2
purpose = "Kitty Graphics Protocol leaf utilities: APC transmission (transmit/delete images), glyph rasterization (ab_glyph + imageproc), and anchor/positioning math. Pure infrastructure consumed by the ui-parts cell (word, progress, background)."
provides = [
  "rasterize_word",
  "transmit_graphics",
  "encode_image_base64",
  "calculate_start_x",
  "delete_image",
]
requires = ["rendering"]
